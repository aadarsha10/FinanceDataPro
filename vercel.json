{
  "version": 2,
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist/client",
  "framework": "vite",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"

    }
  ]
}